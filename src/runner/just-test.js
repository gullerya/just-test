import { getEnvironmentConfig } from './environment-config.js';

export {
	suite
}

function suite(suiteName, suiteOptions) {
	//	when setup mode - setup
	//	when run mode - run the tests
}

function test(testName, testOptions, testCode) {
	//	when setup mode - do configuration
	//	when run mode - run
}